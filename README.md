open graph cache flusher
======


# Installation
`npm install -g ogflush`

# Usage
```bash
ogflush -u "http://some.url/"
ogflush -f file.txt
```

# Supported networks
`facebook` (facebook.com)
`vk` (vk.com)
